1. Enter the following command in the console to run pytest with coverage info
    ```
    py -m pytest -v --cov
    ```
2. Enter the following command in the console to run pytest with coverage info to html
    ```
    py -m pytest -v --cov . --cov-report html
    ```